# Local Environment Overrides Template
# =====================================
# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and will not be committed.
#
# Only include variables you want to override from .env

# =============================================================================
# Required: Change these passwords!
# =============================================================================
# Services link password (must match on both sides)
IRCD_SERVICES_CONNECT_PASSWORD=your-secret-password
X3_GENERAL_CONNECT_PASSWORD=your-secret-password
X3_UPLINK_PASSWORD=your-secret-password

# =============================================================================
# Network Identity (customize for your network)
# =============================================================================
IRCD_GENERAL_NAME=testnet.yourdomain.net
IRCD_ADMIN_CONTACT=admin@yourdomain.net
X3_GENERAL_DOMAIN="yourdomain.net"

# =============================================================================
# Keycloak Database (default uses built-in H2, no setup needed)
# =============================================================================
# To use PostgreSQL instead of H2, uncomment KC_DB and configure connection:

# Option 1: External PostgreSQL (e.g., via Consul DNS for HA)
# KC_DB=postgres
# KC_DB_URL=jdbc:postgresql://master.postgresql.service.consul:5432/db_keycloak?target_session_attrs=read-write
# KC_DB_USERNAME=keycloak
# KC_DB_PASSWORD=your-database-password

# Option 2: Local containerized PostgreSQL (add postgres service to docker-compose.override.yml)
# KC_DB=postgres
# KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak
# KC_DB_USERNAME=keycloak
# KC_DB_PASSWORD=devpassword

# Keycloak external hostname (your host IP or domain)
KC_HOSTNAME=192.168.1.100

# Webhook secret for X3 integration
KC_SPI_EVENTS_LISTENER_WEBHOOK_EVENTS_SECRET=your-webhook-secret

# =============================================================================
# LDAP (optional - uncomment and configure if using LDAP)
# =============================================================================
# X3_LDAP_ENABLE=1
# X3_LDAP_URI=ldaps://ldap.yourdomain.net:636
# X3_LDAP_BASE=DC=ldap,DC=yourdomain,DC=net
# X3_LDAP_DN_FMT=cn=%s,ou=users,dc=yourdomain,dc=net
# X3_LDAP_ADMIN_DN=cn=admin,ou=users,dc=yourdomain,dc=net
# X3_LDAP_ADMIN_PASS=your-ldap-admin-password
